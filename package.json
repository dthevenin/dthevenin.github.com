{
  "name": "web_site",
  "version": "1.0.0",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "6.18.2",
    "babel-preset-es2015": "6.18.0",
    "eslint": "^3.14.1",
    "eslint-config-airbnb": "^14.0.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-jsx-a11y": "^3.0.2",
    "eslint-plugin-react": "^6.9.0",
    "gh-pages": "~0.12.0",
    "less": "^2.7.2"
  },
  "scripts": {
    "docker-dev": "jekyll serve --config /srv/jekyll/configs/dev.yml -s /srv/jekyll/site --incremental",
    "assemble": "rm -rf dist && mkdir dist && cp -r site/* dist/ && cp configs/dev.yml dist/_config.yml",
    "deploy": "yarn run assemble && gh-pages --dist dist --remote origin"
  },
  "babel": {
    "plugins": [
      [ "transform-es2015-modules-amd" ]
    ],
    "presets": [
      "es2015"
    ]
  },
  "author": "David Thevenin",
  "license": "ISC"
}